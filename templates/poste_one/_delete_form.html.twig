<form method="post" action="{{ path('app_poste_one_delete', {'id': poste_one.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cette réservation?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ poste_one.id) }}">
    <button class="btnAction">Supprimer</button>
</form>
