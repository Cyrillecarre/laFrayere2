{% extends 'base.html.twig' %}

{% block title %}PosteTwo index{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/poste.css') }}">
{% endblock %}

{% block body %}
    <h1 class="titre">Poste 2</h1>

    <table class="table">
        <thead>
            <tr class="tableIndex">
                <th>Arrivée</th>
                <th>Départ</th>
                <th>Email</th>
                <th>Tel</th>
                <th>Validé</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for poste_two in poste_twos %}
            <tr class="tableValeur">
                <td>{{ poste_two.start ? poste_two.start|date('d-m-Y H:i:s') : '' }}</td>
                <td>{{ poste_two.end ? poste_two.end|date('d-m-Y H:i:s') : '' }}</td>
                <td>{{ poste_two.email }}</td>
                <td>{{ poste_two.phoneNumber }}</td>
                <td>{{ poste_two.isApprouved ? 'Oui' : 'Non' }}</td>
                <td>
                    <a href="{{ path('app_poste_two_show', {'id': poste_two.id}) }}" class="btnAction">Voir</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_poste_two_new') }}" class="btnAction">Ajouter une nouvelle réservation</a>
    <a href="{{ path('app_admin') }}" class="btnAction">Retour</a>
{% endblock %}
